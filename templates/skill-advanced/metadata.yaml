# Machine-readable metadata. Consumed by tests/, generators/, catalog index.
# Must agree with SKILL.md frontmatter. Schema of record: shared/schemas/.
name: {{NAME}}
title: {{TITLE}}
description: {{DESCRIPTION}}
type: skill
category: {{CATEGORY}}
version: {{VERSION}}
min_standard: {{MIN_STANDARD}}
runtime: {{RUNTIME}}
license: MIT
author: {{AUTHOR}}
created: "{{DATE}}"
updated: "{{DATE}}"
tags: {{TAGS}}
status: active
# Advanced skills often declare a runtime/toolchain for their helper scripts.
requires:
  scripts_runtime: sh        # e.g. sh | python | node — keep portable per ARCHITECTURE §9
  binaries: []               # external CLI tools the scripts assume, if any
# References to other artifacts this skill depends on (name + min version).
dependencies: []
