# Machine-readable metadata. Schema of record: shared/schemas/.
name: {{NAME}}
title: {{TITLE}}
description: {{DESCRIPTION}}
type: agent
version: {{VERSION}}
min_standard: {{MIN_STANDARD}}
runtime: {{RUNTIME}}
license: MIT
author: {{AUTHOR}}
created: "{{DATE}}"
updated: "{{DATE}}"
tags: {{TAGS}}
status: active
# Skills this agent wields (name + min version). Validated by tests/links/.
uses_skills:
  - name: {{CATEGORY}}/{{SKILL}}
    min_version: {{VERSION}}
